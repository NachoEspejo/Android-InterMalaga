<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.intermalaga.nacho.intermalaga">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- PRINCIPAL -->
        <activity android:name=".Actividades.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MENU -->
        <activity
            android:name=".Actividades.ListActivity"
            android:autoRemoveFromRecents="true" />
        <activity android:name=".Actividades.TeamActivity" />
        <activity android:name=".Actividades.CrearPlayers"></activity>
        <activity android:name=".Actividades.RegisterActivity"></activity>
        <activity android:name=".Actividades.InfoPlayer"></activity>
    </application>

</manifest>